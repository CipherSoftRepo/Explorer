For API:
 - just start solution via Visual Studio or .netcore CLI

For web app:
 - requires nodejs (npm)
 - npm install
 - npm start
 
 And that is it.
 
 Using SQLite db so initially will be empty and there is no need for any setup.